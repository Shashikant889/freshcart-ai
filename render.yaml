services:
  # FreshCart AI Web Service on Render Free Tier
  - type: web
    name: freshcart-ai
    runtime: node
    plan: free
    buildCommand: npm install && node db/seed.js
    startCommand: node server.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production

